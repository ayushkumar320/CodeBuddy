# Claude Desktop Example

This example will show CodeBuddy running as an MCP stdio server for Claude Desktop.

Implementation comes in Phase 7 after the SDK, MCP server, CLI, and storage layers are in place.
